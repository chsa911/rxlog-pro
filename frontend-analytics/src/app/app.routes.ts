import { Routes } from '@angular/router';
import { PublicBooksPageComponent } from './public-books-page.component';

export const routes: Routes = [
  { path: '', component: PublicBooksPageComponent },
];
